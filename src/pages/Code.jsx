import {useState} from 'react';import {loadDB,upsertSnippet,searchSnippets} from '../storage/db.js';export default function Code(){const [q,setQ]=useState('');const [items,setItems]=useState(searchSnippets());function save(){upsertSnippet({id:crypto.randomUUID(),name:'Novo snippet',description:'reutilizável',code:'// cole seu código aqui',tags:['reutilizavel'],createdAt:Date.now()});setItems(searchSnippets(q))}return <div><div className="composer"><input className="input" value={q} onChange={e=>{setQ(e.target.value);setItems(searchSnippets(e.target.value))}} placeholder="Pesquisar código/tags..."/><button className="btn primary" onClick={save}>Salvar snippet</button></div><div className="grid">{items.map(x=><div className="card" key={x.id}><b>{x.name}</b><p className="muted">{x.description}</p><pre>{x.code}</pre><span className="muted">#{(x.tags||[]).join(' #')}</span></div>)}</div></div>}
